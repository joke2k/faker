# **POSTCODE_IN_STATE** function

Returns a random postcode for the specified state abbreviation.

```py
from faker import Faker

fake = Faker()

print(fake.postcode_in_state())

# result : 05103
```
