# **PASSPORT_OWNER** function

Returns a tuple containing a given name and surname based on the specified gender marker.

```py
from faker import Faker

fake = Faker()

print(fake.passport_owner())

# result : ('Gary', 'Rubio')
```
